# Documentation Assets

This folder is reserved for small, public-safe diagrams and icons used by the
documentation. Do not add participant-level images, ROI masks, NIfTI files,
raw data, or generated neuroimaging derivatives.

The current pipeline overview is rendered directly from Mermaid source in the
root README, so no separate binary flowchart is required.
